export enum Events {
  MonitorPermissions = "monitor-permissions",
  ClosedStandaloneListBox = "closed_standalone_listbox",
  StartRecordingDockOpened = "start-recording-dock-opened",
  SystemAudioStreamError = "system_audio_stream_error",
}
